/**
 * Function Calls
 * Nadia Abdul Aziz
 * 
 * Practice examples
 */

"use strict";


function setup() {
    //executes once at the beginning of the program
    //Press tab before writing
    //pre-written code, do not need to know how it works
    //double slash tells computer not to read
    //CASE SENSITIVE!!!! CMD WILL NOT BE VALID WITH WRONG CAPS!!
    //width x height in px
    //semicolon = command finished
    //tells computer command and brackets tells what to do for command
    createCanvas(640, 480);
}

function draw() {
    //Once at the beginning of the program
    // RGB values from 255 to 0
    // Spaces after the comma
    //SHOULD HAVE LINE ON LEFT!!! CODE WILL NOT RUN IF NOT
    //Why background is every frame but canvas is not?
    background(100, 200, 200);
}